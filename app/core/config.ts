export let CONFIG = {
  baseUrls: {
    config: 'commands/config',
    resetDb: 'commands/resetDb'//,
 //   characters: 'api/characters.json',
 //  vehicles: 'api/vehicles.json'
  }
};

//used like this: let vehiclesUrl = CONFIG.baseUrls.vehicles
